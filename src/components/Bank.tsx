import React from 'react';
import { Flex } from './Flex';

export const Bank = ({ size }) => (
  <Flex
    position="absolute"
    right={0}
    width="100px"
    border="1px dashed"
    height="100%"
    alignItems="center"
    justifyContent="center"
  >
    Bank
  </Flex>
);
